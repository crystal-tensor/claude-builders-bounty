name: Weekly Dev Summary

on:
  schedule:
    - cron: '0 9 * * 5'  # Friday at 9am UTC
  workflow_dispatch:

jobs:
  summary:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Fetch GitHub activity
        id: activity
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
        run: |
          # Fetch commits, issues, PRs from last 7 days
          echo "Fetching GitHub activity..."
      
      - name: Generate summary
        run: |
          # This workflow works with the n8n workflow
          # For standalone Claude Code usage, see the README
          echo "Setup complete. See README for details."

